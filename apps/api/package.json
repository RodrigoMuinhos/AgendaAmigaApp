{
  "name": "@agenda-amiga/api",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "db:format": "prisma format --schema=./prisma/schema.prisma",
    "db:generate": "prisma generate --schema=./prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema=./prisma/schema.prisma",
    "db:deploy": "prisma migrate deploy --schema=./prisma/schema.prisma",
    "db:reset": "prisma migrate reset --schema=./prisma/schema.prisma --force",
    "db:status": "prisma migrate status --schema=./prisma/schema.prisma",
    "db:studio": "prisma studio --schema=./prisma/schema.prisma",
    "dev": "nodemon -r ts-node/register/transpile-only -r tsconfig-paths/register src/index.ts",
    "prebuild": "npm run db:generate",
    "build": "tsc -p tsconfig.json",
    "postbuild": "",
    "start": "node dist/index.js",
    "render:build": "npm ci && npm run db:generate && npm run build",
    "render:start": "npm run db:deploy && npm run start",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --reporter=dot",
    "test:watch": "vitest",
    "test:cov": "vitest --run --coverage"
  },
  "dependencies": {
    "@agenda-amiga/shared": "file:../../packages/shared",
    "@prisma/client": "^6.18.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "helmet": "^6.2.0",
    "jsonwebtoken": "^9.0.2",
    "socket.io-client": "^4.8.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20.11.0",
    "@types/pg": "^8.6.6",
    "@types/supertest": "^2.0.16",
    "@types/swagger-jsdoc": "^6.0.1",
    "@types/swagger-ui-express": "^4.1.6",
    "@vitest/coverage-v8": "^1.6.0",
    "dotenv-cli": "^11.0.0",
    "nodemon": "^3.1.10",
    "pg": "^8.16.3",
    "prisma": "^6.18.0",
    "supertest": "^7.1.4",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
