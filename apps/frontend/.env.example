# Base da API HTTP (dev local / prod)
VITE_API_HTTP_BASE=http://localhost:3333
# Caminho do websocket (se usar Socket.IO no backend)
VITE_API_WS_PATH=/socket.io
# Opcional: chaves públicas de serviços externos
# VITE_SENTRY_DSN=
# VITE_POSTHOG_KEY=
