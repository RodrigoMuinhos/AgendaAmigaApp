version: "3.8"
services:
  api:
    image: agenda-amiga:latest
    build:
      context: .
      target: production
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
